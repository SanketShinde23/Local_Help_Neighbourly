# Frontend (Vite) — copy to `.env.local` for local dev overrides
VITE_API_URL=https://local-help-neighbourly.onrender.com

# Local backend (optional, for development only):
# VITE_API_URL=http://localhost:5000
